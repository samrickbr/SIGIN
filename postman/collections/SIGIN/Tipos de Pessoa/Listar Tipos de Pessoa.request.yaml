$kind: http-request
name: Listar Tipos de Pessoa
method: GET
url: '{{baseUrl}}/tipos-pessoa'
order: 1000
scripts:
  - type: afterResponse
    language: text/javascript
    code: |-
      pm.test("Status 200", () => pm.response.to.have.status(200));
      pm.test("Resposta é array", () => {
        pm.expect(pm.response.json()).to.be.an('array');
      });
