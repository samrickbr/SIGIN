$kind: http-request
name: Listar Ordens de Producao
method: GET
url: '{{baseUrl}}/ordens-producao'
order: 1000
scripts:
  - type: afterResponse
    language: text/javascript
    code: |-
      pm.test("Status 200", () => pm.response.to.have.status(200));
      pm.test("Resposta é array", () => {
        pm.expect(pm.response.json()).to.be.an('array');
      });
