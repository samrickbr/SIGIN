$kind: http-request
name: Iniciar Ordem de Producao
method: POST
url: '{{baseUrl}}/ordens-producao/{{ordemProducaoId}}/iniciar'
order: 4000
scripts:
  - type: afterResponse
    language: text/javascript
    code: |-
      pm.test("Status 200", () => pm.response.to.have.status(200));
